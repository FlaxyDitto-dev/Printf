# Printf
## A command that will change the world!
This is a command that is open-source, it is really close to the syntax of the echo command but it is better. It can be used like this: `printf "Hello, World"`.
  But the best features are support for escape codes, unicodes, ANSI colors and more.
